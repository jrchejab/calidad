# Project decisions — portal_calidad

Decisiones que no se repiten. Una línea por decisión con fecha.
Política: solo repetible, máximo ~30 líneas, obsoleto se marca no se borra. Nada de secrets.

## Formato

- `YYYY-MM-DD` — decisión — motivo corto.

## Decisiones

- `2026-09-11` — Rukovoditel como appliance Docker sin código propio, solo empaque — gestión interna.
- `2026-09-11` — esquema `solo-prod` rama `única: master` en `VPS-01` — sin sandbox.
- `2026-09-11` — dump de migración fuera del repo en `Temp\opencode\`, datos ya en volumen — repo sin datos.
